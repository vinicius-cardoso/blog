# build output
dist/
.astro/

# dependencies
node_modules/

# environment — never commit. This repo is public.
.env
.env.*

# local deploy config (server address lives here, not in the repo)
deploy/local.conf

# editor / OS
.vscode/
.idea/
.DS_Store
*.swp

# notes about other machines/projects — kept locally, not published
notes/
