# build output
dist/
.astro/

# dependencies
node_modules/

# environment — never commit. Holds the admin password hash, the session
# secret and the real host/domain. This repo is public.
.env
.env.*
!.env.example

# admin runtime state
admin/blog.db
admin/blog.db-*
admin/uploads/
admin/bin/

# local deploy config (server address lives here, not in the repo)
deploy/local.conf

# editor / OS
.vscode/
.idea/
.DS_Store
*.swp

# notes about other machines/projects — kept locally, not published
notes/
